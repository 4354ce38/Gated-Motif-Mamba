Base model files are not bundled in this GitHub repo.

Planned:
- the base Mamba-2 130M model will be uploaded to Hugging Face later

For now:
- download the base model separately
- place it in `assets/models/mamba2-130m/`
- or pass `--model-dir /path/to/model` to the scripts
